import { libsSharedAppTypes } from './libs-shared-app-types';

describe('libsSharedAppTypes', () => {
  it('should work', () => {
    expect(libsSharedAppTypes()).toEqual('libs-shared-app-types');
  });
});
